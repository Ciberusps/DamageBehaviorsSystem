# DamageBehaviorsSystem v2 Plan

- Test'ы с покрытием юзкейсов основных - сделать до любых фич
- GameplayDebugCategory
- идентификация теперь только по тегам. То есть хитарегистраторы обладают тегами. Source'ы, с которых мы берем, тоже обладают тегами.   то есть и с этим к суть с этим судле потом так как и идентификатором то что добавлю это то что по нейму дыма шпи хета все в 2 удаляется потом следующее 
- Sources
  - вводится C++ native way взять source, это source finder class. Это на плюсах нативный способ без инстанцирования объекта найти source
  - теперь живут на DamageBehaviorComponent, т.е. объявляются на компоненте самом, возможно берутся через интерфейс но по дефолту берутся с компонента наверно
  - DBSSourcesDataAsset - способ шарить Source'ы между компонентами, чтобы не плодить их на каждом компоненте. Внутри DataAsset'а можно будет выбрать набор Source'ов, которые будут использоваться на компоненте. Source'ы возможность выбора набора через DataAsset и прямо на компоненте
- возможность редиректа демеджа в другой actor/actorcomponent любую хрень имплементящую интерфейс
- Debug - отрисовка хитбоксов в анимациях
